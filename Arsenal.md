# Transformar lista para inteiros

```python
    #Responsável por transformar uma lista de strings em inteiros
    # Entrada:  200 200 200 250
    lista_inteiros = list(map(int, input().split(" ")))
```
# Transformar lista em string
```python
    #Transformar lista em string
    frase_completa = " ".join(lista)
```

# Criar lista removendo todos os espaços
```python
    # Carlos       Carlos -> ['Carlos', 'Carlos']
    lista_sem_espacos = string.split()
```












